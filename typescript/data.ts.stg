main(m) ::= <<
// Generated by kibo from <m.namespace>. Do not edit by hand.
// Templates: kibo-template-viper 2.0.0 (MIT), Template Model 2.
// Runtime: this file imports `@digitalsubstrate/dsviper` >=1.2.8 \<1.3.0,
// distributed under LicenseRef-DigitalSubstrate-Commercial-1.2.
// Commercial use requires a Commercial Licence from Digital Substrate.
// <m.generated>

/**
 * TypeScript classes for concepts, clubs, enums, structs and containers of <m.namespace>.
 */

import dsviper from "@digitalsubstrate/dsviper";
import * as mt from "./value_type.js";
import * as md from "./definitions.js";

export class Proxy\<V extends dsviper.Value = dsviper.Value> {

    readonly vprValue: V;

    constructor(vprValue: V) {
        this.vprValue = vprValue;
    }

    equals(other: Proxy): boolean {
        return this.vprValue.equals(other.vprValue);
    }

    compareTo(other: Proxy): number {
        return this.vprValue.compare(other.vprValue);
    }

    toString(): string {
        return this.vprValue.toString();
    }

    hexdigest(): string {
        return dsviper.Value.hexdigest(this.vprValue);
    }

    encode(streamCodecInstancing?: dsviper.StreamCodecInstancing): dsviper.ValueBlob {
        return dsviper.Value.encode(this.vprValue, streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY);
    }

    write(streamWriting: dsviper.StreamWriting): void {
        dsviper.Value.write(this.vprValue, streamWriting);
    }
}

// AnyConceptKey
export class AnyConceptKey extends Proxy\<dsviper.ValueKey> {

    constructor(value: dsviper.ValueKey) {
        if (!value.typeKey().isAnyConcept()) {
            throw new TypeError("value is not a Key for any_concept");
        }
        super(value);
    }

    static wrap(value: dsviper.OutputValue): AnyConceptKey {
        return new AnyConceptKey(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [<m.concepts:{c|md.RuntimeIds.<c.bindingType.proxy>};separator=", ">].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        <m.concepts:any_concept_description();separator="\n">
        return `${instance}:AnyConceptKey(${rid.encoded()})`;
    }

    toString(): string {
        return this.description();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): AnyConceptKey {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type_AnyConceptKey(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new AnyConceptKey(value);
    }
}

<m.nameSpaces:namespace();separator="\n">
<m.vecFunctions:vec();separator="\n">
<m.matFunctions:mat();separator="\n">
<m.tupleFunctions:tuple();separator="\n">
<m.optionalFunctions:optional();separator="\n">
<m.vectorFunctions:vector();separator="\n">
<m.setFunctions:set();separator="\n">
<m.mapFunctions:map();separator="\n">
<m.xarrayFunctions:xarray();separator="\n">
<m.variantFunctions:variant();separator="\n">
>>

namespace(ns) ::= <<
// Namespace <ns.name>
<ns.concepts:concept();separator="\n">
<ns.clubs:club();separator="\n">
<ns.enumerations:enumeration();separator="\n">
<ns.structures:structure();separator="\n">
>>

any_concept_description(c) ::= <<
if (rid.equals(md.RuntimeIds.<c.bindingType.proxy>)) { return `${instance}:AnyConceptKey(<c.dsmType>)`; }
>>

// Leaf primitive mapping (§3): only this dictionary is language-specific.
tsType(t) ::= <%<if(t.useProxy)><t.proxy><else><t.type><endif>%>
unwrap(t, e) ::= <%<if(t.useProxy)><e>.vprValue<else><e><endif>%>
wrap(t, e) ::= <%<if(t.useProxy)><t.proxy>.wrap(<e>)<else><e> as <t.type><endif>%>

enumeration(e) ::= <<
// enum <e.dsmType>
export class <e.bindingType.proxy> extends Proxy\<dsviper.ValueEnumeration> {

    constructor(value: dsviper.ValueEnumeration) {
        if (!value.type().equals(mt.type<e.typeSuffix>())) {
            throw new TypeError("value is not a <e.dsmType>");
        }
        super(value);
    }

    static wrap(value: dsviper.OutputValue): <e.bindingType.proxy> {
        return new <e.bindingType.proxy>(dsviper.ValueEnumeration.cast(value as dsviper.Value));
    }

    name(): string {
        return this.vprValue.name();
    }

    <e.members:enumeration_case(e);separator="\n">

    static fromStr(name: string): <e.bindingType.proxy> {
        switch (name) {
            <e.members:enumeration_name(e);separator="\n">
            default:
                throw new Error(`unknown enumeration case ${name}`);
        }
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <e.bindingType.proxy> {
        const value = dsviper.ValueEnumeration.cast(dsviper.Value.decode(blob, mt.type<e.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <e.bindingType.proxy>(value);
    }
}

>>

enumeration_case(em, e) ::= <<
private static _<em.name;format="usc">: <e.bindingType.proxy> | null = null;
static get <em.name;format="usc">(): <e.bindingType.proxy> {
    if (<e.bindingType.proxy>._<em.name;format="usc"> === null) {
        <e.bindingType.proxy>._<em.name;format="usc"> = new <e.bindingType.proxy>(new dsviper.ValueEnumeration(mt.type<e.typeSuffix>(), "<em.name>"));
    }
    return <e.bindingType.proxy>._<em.name;format="usc">;
}
>>

enumeration_name(em, e) ::= <<
case "<em.name>":
    return <e.bindingType.proxy>.<em.name;format="usc">;
>>

structure(s) ::= <<
// struct <s.dsmType>
export class <s.bindingType.proxy> extends Proxy\<dsviper.ValueStructure> {

    constructor(value?: dsviper.ValueStructure | Record\<string, dsviper.InputValue>) {
        if (value instanceof dsviper.ValueStructure) {
            if (!value.type().equals(mt.type<s.typeSuffix>())) {
                throw new TypeError("value is not a <s.dsmType>");
            }
            super(value);
        } else {
            super(new dsviper.ValueStructure(mt.type<s.typeSuffix>(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): <s.bindingType.proxy> {
        return new <s.bindingType.proxy>(dsviper.ValueStructure.cast(value as dsviper.Value));
    }

    <s.fields:structure_field();separator="\n">

    copy(): <s.bindingType.proxy> {
        return new <s.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <s.bindingType.proxy> {
        const value = dsviper.ValueStructure.cast(dsviper.Value.decode(blob, mt.type<s.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <s.bindingType.proxy>(value);
    }
}

>>

structure_field(sf) ::= <<
get <sf.name>(): <sf.bindingType:tsType()> {
    return <sf.bindingType:wrap({this.vprValue.at("<sf.name>")})>;
}

set <sf.name>(value: <sf.bindingType:tsType()>) {
    this.vprValue.set("<sf.name>", <sf.bindingType:unwrap("value")>);
}
>>

concept(c) ::= <<
// concept <c.dsmType>
export class <c.bindingType.proxy>Key extends Proxy\<dsviper.ValueKey> {

    constructor(identifier: dsviper.ValueKey | dsviper.ValueUUId | string) {
        if (identifier instanceof dsviper.ValueKey) {
            if (!identifier.type().equals(mt.type<c.typeSuffix>())) {
                throw new TypeError("identifier is not a <c.dsmType>");
            }
            super(identifier);
        } else if (identifier instanceof dsviper.ValueUUId || typeof identifier === "string") {
            super(dsviper.ValueKey.create(mt.type_check<c.typeSuffix>(), identifier));
        } else {
            throw new TypeError("identifier must be a ValueKey, a ValueUUId or a string");
        }
    }

    static wrap(value: dsviper.OutputValue): <c.bindingType.proxy>Key {
        return new <c.bindingType.proxy>Key(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    static create(): <c.bindingType.proxy>Key {
        return new <c.bindingType.proxy>Key(dsviper.ValueUUId.create());
    }
    <if(c.parent)>

    toParentKey(): <c.parent.bindingType.proxy>Key {
        return new <c.parent.bindingType.proxy>Key(this.vprValue.toParentKey());
    }
    <endif>
    <c.strictDescendants:concept_descendant_getter();separator="\n">
    <c.strictDescendants:concept_descendant_constructor(c);separator="\n">

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [<c.descendants:{cd|md.RuntimeIds.<cd.bindingType.proxy>};separator=", ">].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        <c.strictDescendants:concept_descendant_description(c);separator="\n">
        if (rid.equals(md.RuntimeIds.<c.bindingType.proxy>)) { return `${instance}:<c.dsmType>`; }
        return `${instance}:<c.dsmType>(${rid.encoded()})`;
    }

    toString(): string {
        return this.description();
    }

    static fromAnyConceptKey(key: AnyConceptKey): <c.bindingType.proxy>Key | null {
        const rid = key.runtimeId();
        if (rid.equals(md.RuntimeIds.<c.bindingType.proxy>)) {
            return new <c.bindingType.proxy>Key(key.instanceId());
        }
        return null;
    }

    toAnyConceptKey(): AnyConceptKey {
        return new AnyConceptKey(this.vprValue.toAnyConceptKey());
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <c.bindingType.proxy>Key {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type<c.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <c.bindingType.proxy>Key(value);
    }
}

>>

concept_descendant_getter(cd) ::= <<
as<cd.name>Key(): <cd.bindingType.proxy>Key | null {
    if (this.vprValue.isMember(mt.type_check<cd.typeSuffix>())) {
        return new <cd.bindingType.proxy>Key(this.vprValue.toMemberKey(mt.type_check<cd.typeSuffix>()));
    }
    return null;
}
>>

concept_descendant_constructor(cd, c) ::= <<
static from<cd.name>Key(key: <cd.bindingType.proxy>Key): <c.bindingType.proxy>Key {
    return new <c.bindingType.proxy>Key(key.vprValue.toMemberKey(mt.type_check<c.typeSuffix>()));
}
>>

concept_descendant_description(cd, c) ::= <<
if (rid.equals(md.RuntimeIds.<cd.bindingType.proxy>)) { return `${instance}:<c.dsmType>(<cd.dsmType>)`; }
>>

club(c) ::= <<
// club <c.dsmType>
export class <c.bindingType.proxy>Key extends Proxy\<dsviper.ValueKey> {

    constructor(value: dsviper.ValueKey) {
        if (!value.type().equals(mt.type<c.typeSuffix>())) {
            throw new TypeError("value is not a <c.dsmType>");
        }
        super(value);
    }

    static wrap(value: dsviper.OutputValue): <c.bindingType.proxy>Key {
        return new <c.bindingType.proxy>Key(dsviper.ValueKey.cast(value as dsviper.Value));
    }

    <c.members:club_getter();separator="\n">
    <c.members:club_constructor(c);separator="\n">

    toAnyConceptKey(): AnyConceptKey {
        return new AnyConceptKey(this.vprValue.toAnyConceptKey());
    }

    instanceId(): dsviper.ValueUUId {
        return this.vprValue.instanceId();
    }

    runtimeId(): dsviper.ValueUUId {
        return this.vprValue.typeConcept().runtimeId();
    }

    isValid(): boolean {
        return this.instanceId().isValid();
    }

    isKnown(): boolean {
        const rid = this.runtimeId();
        return [<c.memberDescendants:{cd|md.RuntimeIds.<cd.bindingType.proxy>};separator=", ">].some((id) => rid.equals(id));
    }

    description(): string {
        const rid = this.runtimeId();
        const instance = this.instanceId().encoded();
        <c.memberDescendants:club_member_descendant_description(c);separator="\n">
        return `<c.dsmType>(${instance})`;
    }

    toString(): string {
        return this.description();
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <c.bindingType.proxy>Key {
        const value = dsviper.ValueKey.cast(dsviper.Value.decode(blob, mt.type<c.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <c.bindingType.proxy>Key(value);
    }
}

>>

club_getter(cm) ::= <<
as<cm.name>Key(): <cm.bindingType.proxy>Key | null {
    if (this.runtimeId().equals(md.RuntimeIds.<cm.bindingType.proxy>)) {
        return new <cm.bindingType.proxy>Key(dsviper.ValueKey.create(mt.type_check<cm.typeSuffix>(), this.instanceId()).toMemberKey(mt.type_check<cm.typeSuffix>()));
    }
    return null;
}
>>

club_constructor(cm, c) ::= <<
static from<cm.name>Key(key: <cm.bindingType.proxy>Key): <c.bindingType.proxy>Key {
    return new <c.bindingType.proxy>Key(key.vprValue.toClubKey(mt.type_check<c.typeSuffix>()));
}
>>

club_member_descendant_description(cd, c) ::= <<
if (rid.equals(md.RuntimeIds.<cd.bindingType.proxy>)) { return `${instance}:<c.dsmType>(<cd.dsmType>)`; }
>>

vec(v) ::= <<
// <v.dsmType>
export class <v.bindingType.proxy> extends Proxy\<dsviper.ValueVec> {

    constructor(value?: dsviper.ValueVec | <v.bindingSequenceType>) {
        if (value instanceof dsviper.ValueVec) {
            if (!value.type().equals(mt.type<v.typeSuffix>())) {
                throw new TypeError("value is not a <v.dsmType>");
            }
            super(value);
        } else {
            super(new dsviper.ValueVec(mt.type<v.typeSuffix>(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(dsviper.ValueVec.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.at(index)})>;
    }

    set(index: number, value: <v.bindingElementType:tsType()>): void {
        this.vprValue.set(index, <v.bindingElementType:unwrap("value")>);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield <v.bindingElementType:wrap("v")>;
        }
    }

    toArray(): <v.bindingSequenceType> {
        return this.vprValue.toArray() as <v.bindingSequenceType>;
    }

    copy(): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <v.bindingType.proxy> {
        const value = dsviper.ValueVec.cast(dsviper.Value.decode(blob, mt.type<v.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <v.bindingType.proxy>(value);
    }
}

>>

mat(v) ::= <<
// <v.dsmType>
export class <v.bindingType.proxy> extends Proxy\<dsviper.ValueMat> {

    constructor(value?: dsviper.ValueMat | <v.bindingSequenceType>) {
        if (value instanceof dsviper.ValueMat) {
            if (!value.type().equals(mt.type<v.typeSuffix>())) {
                throw new TypeError("value is not a <v.dsmType>");
            }
            super(value);
        } else {
            super(new dsviper.ValueMat(mt.type<v.typeSuffix>(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(dsviper.ValueMat.cast(value as dsviper.Value));
    }

    get columns(): number {
        return this.vprValue.columns();
    }

    get rows(): number {
        return this.vprValue.rows();
    }

    get size(): number {
        return this.vprValue.columns() * this.vprValue.rows();
    }

    at(row: number, column: number): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.at(row, column)})>;
    }

    set(row: number, column: number, value: <v.bindingElementType:tsType()>): void {
        this.vprValue.set(row, column, <v.bindingElementType:unwrap("value")>);
    }

    row(row: number): <v.bindingColumnType> {
        return (this.vprValue.toArray() as <v.bindingSequenceType>)[row];
    }

    setRow(row: number, values: <v.bindingColumnType>): void {
        values.forEach((value, column) => this.vprValue.set(row, column, value));
    }

    toArray(): <v.bindingSequenceType> {
        return this.vprValue.toArray() as <v.bindingSequenceType>;
    }

    copy(): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <v.bindingType.proxy> {
        const value = dsviper.ValueMat.cast(dsviper.Value.decode(blob, mt.type<v.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <v.bindingType.proxy>(value);
    }
}

>>

tuple(v) ::= <<
// <v.dsmType>
export class <v.bindingType.proxy> extends Proxy\<dsviper.ValueTuple> {

    constructor(value?: dsviper.ValueTuple | dsviper.InputValue[]) {
        if (value instanceof dsviper.ValueTuple) {
            if (!value.type().equals(mt.type<v.typeSuffix>())) {
                throw new TypeError("value is not a <v.dsmType>");
            }
            super(value);
        } else {
            super(new dsviper.ValueTuple(mt.type<v.typeSuffix>(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(dsviper.ValueTuple.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): dsviper.OutputValue {
        return this.vprValue.at(index);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield v;
        }
    }

    copy(): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <v.bindingType.proxy> {
        const value = dsviper.ValueTuple.cast(dsviper.Value.decode(blob, mt.type<v.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <v.bindingType.proxy>(value);
    }
}

>>

optional(v) ::= <<
// <v.dsmType>
export class <v.bindingType.proxy> extends Proxy\<dsviper.ValueOptional> {

    constructor(value?: dsviper.ValueOptional | <v.bindingElementType:tsType()> | null) {
        if (value instanceof dsviper.ValueOptional) {
            if (!value.type().equals(mt.type<v.typeSuffix>())) {
                throw new TypeError("value is not a <v.dsmType>");
            }
            super(value);
        } else if (value === null || value === undefined) {
            super(new dsviper.ValueOptional(mt.type<v.typeSuffix>()));
        } else {
            super(new dsviper.ValueOptional(mt.type<v.typeSuffix>(), <v.bindingElementType:unwrap("value")>));
        }
    }

    static wrap(value: dsviper.OutputValue): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(dsviper.ValueOptional.cast(value as dsviper.Value));
    }

    isNil(): boolean {
        return this.vprValue.isNil();
    }

    unwrap(): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.unwrap()})>;
    }

    wrap(value: <v.bindingElementType:tsType()>): void {
        this.vprValue.wrap(<v.bindingElementType:unwrap("value")>);
    }

    get(defaultValue?: <v.bindingElementType:tsType()>): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.get(defaultValue === undefined ? undefined : <v.bindingElementType:unwrap("defaultValue")>)})>;
    }

    copy(): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <v.bindingType.proxy> {
        const value = dsviper.ValueOptional.cast(dsviper.Value.decode(blob, mt.type<v.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <v.bindingType.proxy>(value);
    }
}

>>

vector(v) ::= <<
// <v.dsmType>
export class <v.bindingType.proxy> extends Proxy\<dsviper.ValueVector> {

    constructor(value?: dsviper.ValueVector | dsviper.InputValue) {
        if (value instanceof dsviper.ValueVector) {
            if (!value.type().equals(mt.type<v.typeSuffix>())) {
                throw new TypeError("value is not a <v.dsmType>");
            }
            super(value);
        } else {
            super(new dsviper.ValueVector(mt.type<v.typeSuffix>(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(dsviper.ValueVector.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.at(index)})>;
    }

    set(index: number, value: <v.bindingElementType:tsType()>): void {
        this.vprValue.set(index, <v.bindingElementType:unwrap("value")>);
    }

    append(value: <v.bindingElementType:tsType()>): void {
        this.vprValue.append(<v.bindingElementType:unwrap("value")>);
    }

    insert(index: number, value: <v.bindingElementType:tsType()>): void {
        this.vprValue.insert(index, <v.bindingElementType:unwrap("value")>);
    }

    pop(index?: number): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.pop(index)})>;
    }

    remove(value: <v.bindingElementType:tsType()>): void {
        this.vprValue.remove(<v.bindingElementType:unwrap("value")>);
    }

    contains(value: <v.bindingElementType:tsType()>): boolean {
        return this.vprValue.contains(<v.bindingElementType:unwrap("value")>);
    }

    count(value: <v.bindingElementType:tsType()>): number {
        return this.vprValue.count(<v.bindingElementType:unwrap("value")>);
    }

    index(value: <v.bindingElementType:tsType()>): number {
        return this.vprValue.index(<v.bindingElementType:unwrap("value")>);
    }

    extend(other: <v.bindingType.proxy>): void {
        this.vprValue.extend(other.vprValue);
    }

    concat(other: <v.bindingType.proxy>): <v.bindingType.proxy> {
        const result = new <v.bindingType.proxy>(this.vprValue.copy());
        result.extend(other);
        return result;
    }

    clear(): void {
        this.vprValue.clear();
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield <v.bindingElementType:wrap("v")>;
        }
    }

    copy(): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <v.bindingType.proxy> {
        const value = dsviper.ValueVector.cast(dsviper.Value.decode(blob, mt.type<v.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <v.bindingType.proxy>(value);
    }
}

>>

set(v) ::= <<
// <v.dsmType>
export class <v.bindingType.proxy> extends Proxy\<dsviper.ValueSet> {

    constructor(value?: dsviper.ValueSet | dsviper.InputValue) {
        if (value instanceof dsviper.ValueSet) {
            if (!value.type().equals(mt.type<v.typeSuffix>())) {
                throw new TypeError("value is not a <v.dsmType>");
            }
            super(value);
        } else {
            super(new dsviper.ValueSet(mt.type<v.typeSuffix>(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(dsviper.ValueSet.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(index: number): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.at(index)})>;
    }

    add(value: <v.bindingElementType:tsType()>): void {
        this.vprValue.add(<v.bindingElementType:unwrap("value")>);
    }

    discard(value: <v.bindingElementType:tsType()>): void {
        this.vprValue.discard(<v.bindingElementType:unwrap("value")>);
    }

    remove(value: <v.bindingElementType:tsType()>): void {
        this.vprValue.remove(<v.bindingElementType:unwrap("value")>);
    }

    pop(): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.pop()})>;
    }

    contains(value: <v.bindingElementType:tsType()>): boolean {
        return this.vprValue.index(<v.bindingElementType:unwrap("value")>) !== undefined;
    }

    clear(): void {
        this.vprValue.clear();
    }

    min(): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.min()})>;
    }

    max(): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.max()})>;
    }

    union(other: <v.bindingType.proxy>): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.union(other.vprValue));
    }

    intersection(other: <v.bindingType.proxy>): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.intersection(other.vprValue));
    }

    difference(other: <v.bindingType.proxy>): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.difference(other.vprValue));
    }

    symmetricDifference(other: <v.bindingType.proxy>): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.symmetricDifference(other.vprValue));
    }

    update(other: <v.bindingType.proxy>): void {
        this.vprValue.update(other.vprValue);
    }

    intersectionUpdate(other: <v.bindingType.proxy>): void {
        this.vprValue.intersectionUpdate(other.vprValue);
    }

    differenceUpdate(other: <v.bindingType.proxy>): void {
        this.vprValue.differenceUpdate(other.vprValue);
    }

    symmetricDifferenceUpdate(other: <v.bindingType.proxy>): void {
        this.vprValue.symmetricDifferenceUpdate(other.vprValue);
    }

    isdisjoint(other: <v.bindingType.proxy>): boolean {
        return this.vprValue.isdisjoint(other.vprValue);
    }

    issubset(other: <v.bindingType.proxy>): boolean {
        return this.vprValue.issubset(other.vprValue);
    }

    issuperset(other: <v.bindingType.proxy>): boolean {
        return this.vprValue.issuperset(other.vprValue);
    }

    *[Symbol.iterator]() {
        for (const v of this.vprValue) {
            yield <v.bindingElementType:wrap("v")>;
        }
    }

    copy(): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <v.bindingType.proxy> {
        const value = dsviper.ValueSet.cast(dsviper.Value.decode(blob, mt.type<v.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <v.bindingType.proxy>(value);
    }
}

>>

map(v) ::= <<
// <v.dsmType>
export class <v.bindingType.proxy> extends Proxy\<dsviper.ValueMap> {

    constructor(value?: dsviper.ValueMap | dsviper.InputValue) {
        if (value instanceof dsviper.ValueMap) {
            if (!value.type().equals(mt.type<v.typeSuffix>())) {
                throw new TypeError("value is not a <v.dsmType>");
            }
            super(value);
        } else {
            super(new dsviper.ValueMap(mt.type<v.typeSuffix>(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(dsviper.ValueMap.cast(value as dsviper.Value));
    }

    get size(): number {
        return this.vprValue.size();
    }

    at(key: <v.bindingKeyType:tsType()>): <v.bindingElementType:tsType()> {
        return <v.bindingElementType:wrap({this.vprValue.at(<v.bindingKeyType:unwrap("key")>)})>;
    }

    set(key: <v.bindingKeyType:tsType()>, value: <v.bindingElementType:tsType()>): void {
        this.vprValue.set(<v.bindingKeyType:unwrap("key")>, <v.bindingElementType:unwrap("value")>);
    }

    has(key: <v.bindingKeyType:tsType()>): boolean {
        return this.vprValue.contains(<v.bindingKeyType:unwrap("key")>);
    }

    get(key: <v.bindingKeyType:tsType()>, defaultValue?: <v.bindingElementType:tsType()>): <v.bindingElementType:tsType()> | undefined {
        const value = this.vprValue.get(<v.bindingKeyType:unwrap("key")>, defaultValue === undefined ? undefined : <v.bindingElementType:unwrap("defaultValue")>);
        return value === undefined ? undefined : <v.bindingElementType:wrap("value")>;
    }

    remove(key: <v.bindingKeyType:tsType()>): void {
        this.vprValue.remove(<v.bindingKeyType:unwrap("key")>);
    }

    pop(key: <v.bindingKeyType:tsType()>, defaultValue?: <v.bindingElementType:tsType()>): <v.bindingElementType:tsType()> | undefined {
        const value = this.vprValue.pop(<v.bindingKeyType:unwrap("key")>, defaultValue === undefined ? undefined : <v.bindingElementType:unwrap("defaultValue")>);
        return value === undefined ? undefined : <v.bindingElementType:wrap("value")>;
    }

    popitem(): [<v.bindingKeyType:tsType()>, <v.bindingElementType:tsType()>] {
        const entry = this.vprValue.popitem() as unknown as dsviper.OutputValue[];
        return [<v.bindingKeyType:wrap({entry[0]})>, <v.bindingElementType:wrap({entry[1]})>];
    }

    setdefault(key: <v.bindingKeyType:tsType()>, defaultValue: <v.bindingElementType:tsType()>): void {
        this.vprValue.setdefault(<v.bindingKeyType:unwrap("key")>, <v.bindingElementType:unwrap("defaultValue")>);
    }

    update(other: <v.bindingType.proxy>): void {
        this.vprValue.update(other.vprValue);
    }

    clear(): void {
        this.vprValue.clear();
    }

    *keys() {
        for (const [k] of this.vprValue as unknown as Iterable\<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield <v.bindingKeyType:wrap("k")>;
        }
    }

    *values() {
        for (const [, value] of this.vprValue as unknown as Iterable\<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield <v.bindingElementType:wrap("value")>;
        }
    }

    *entries(): Generator\<[<v.bindingKeyType:tsType()>, <v.bindingElementType:tsType()>]> {
        for (const [k, value] of this.vprValue as unknown as Iterable\<[dsviper.OutputValue, dsviper.OutputValue]>) {
            yield [<v.bindingKeyType:wrap("k")>, <v.bindingElementType:wrap("value")>];
        }
    }

    items(): [<v.bindingKeyType:tsType()>, <v.bindingElementType:tsType()>][] {
        return [...this.entries()];
    }

    *[Symbol.iterator]() {
        yield* this.keys();
    }

    copy(): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <v.bindingType.proxy> {
        const value = dsviper.ValueMap.cast(dsviper.Value.decode(blob, mt.type<v.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <v.bindingType.proxy>(value);
    }
}

>>

xarray(v) ::= <<
// <v.dsmType>
export class <v.bindingType.proxy> extends Proxy\<dsviper.ValueXArray> {

    constructor(value?: dsviper.ValueXArray | dsviper.InputValue) {
        if (value instanceof dsviper.ValueXArray) {
            if (!value.type().equals(mt.type<v.typeSuffix>())) {
                throw new TypeError("value is not a <v.dsmType>");
            }
            super(value);
        } else {
            super(new dsviper.ValueXArray(mt.type<v.typeSuffix>(), value));
        }
    }

    static wrap(value: dsviper.OutputValue): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(dsviper.ValueXArray.cast(value as dsviper.Value));
    }

    static end(): dsviper.ValueUUId {
        return dsviper.ValueXArray.END;
    }

    static createPosition(): dsviper.ValueUUId {
        return dsviper.ValueXArray.createPosition();
    }

    get size(): number {
        return this.vprValue.items().length;
    }

    at(position: dsviper.ValueUUId): <v.bindingElementType:tsType()> | undefined {
        const v = this.vprValue.at(position);
        return v === undefined ? undefined : <v.bindingElementType:wrap("v")>;
    }

    get(key: number | dsviper.ValueUUId): <v.bindingElementType:tsType()> | undefined {
        const position = typeof key === "number" ? this.vprValue.position(key) : key;
        if (position === undefined) { return undefined; }
        return this.at(position);
    }

    set(key: number | dsviper.ValueUUId, value: <v.bindingElementType:tsType()>): void {
        const position = typeof key === "number" ? this.vprValue.position(key) : key;
        if (position === undefined) { return; }
        this.vprValue.set(position, <v.bindingElementType:unwrap("value")>);
    }

    contains(value: <v.bindingElementType:tsType()>): boolean {
        return this.vprValue.positionOf(<v.bindingElementType:unwrap("value")>) !== undefined;
    }

    append(value: <v.bindingElementType:tsType()>): dsviper.ValueUUId {
        return this.vprValue.append(<v.bindingElementType:unwrap("value")>);
    }

    insert(beforePosition: dsviper.ValueUUId, value: <v.bindingElementType:tsType()>, newPosition?: dsviper.ValueUUId): dsviper.ValueUUId {
        if (newPosition === undefined) {
            return this.vprValue.insert(beforePosition, <v.bindingElementType:unwrap("value")>);
        }
        return this.vprValue.insert(beforePosition, <v.bindingElementType:unwrap("value")>, newPosition);
    }

    remove(position: dsviper.ValueUUId): void {
        this.vprValue.remove(position);
    }

    positions(): dsviper.ValueUUId[] {
        return this.vprValue.positions();
    }

    index(position: dsviper.ValueUUId): number | undefined {
        return this.vprValue.index(position);
    }

    position(index: number): dsviper.ValueUUId | undefined {
        return this.vprValue.position(index);
    }

    hasPosition(position: dsviper.ValueUUId): boolean {
        return this.vprValue.hasPosition(position);
    }

    insertPosition(beforePosition: dsviper.ValueUUId, newPosition: dsviper.ValueUUId): void {
        this.vprValue.insertPosition(beforePosition, newPosition);
    }

    disablePosition(position: dsviper.ValueUUId): void {
        this.vprValue.disablePosition(position);
    }

    positionOf(value: <v.bindingElementType:tsType()>): dsviper.ValueUUId | undefined {
        return this.vprValue.positionOf(<v.bindingElementType:unwrap("value")>);
    }

    items(): [dsviper.ValueUUId, <v.bindingElementType:tsType()>][] {
        return this.vprValue.items().map(([position, value]) => [position, <v.bindingElementType:wrap({value as dsviper.OutputValue})>]);
    }

    toVector(): Vector<v.elementTypeSuffix> {
        return new Vector<v.elementTypeSuffix>(this.vprValue.toVector());
    }

    copy(): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <v.bindingType.proxy> {
        const value = dsviper.ValueXArray.cast(dsviper.Value.decode(blob, mt.type<v.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <v.bindingType.proxy>(value);
    }
}

>>

variant(v) ::= <<
// <v.dsmType>
export class <v.bindingType.proxy> extends Proxy\<dsviper.ValueVariant> {

    constructor(value?: dsviper.ValueVariant | Proxy | dsviper.InputValue) {
        if (value === undefined || value === null) {
            super(new dsviper.ValueVariant(mt.type<v.typeSuffix>()));
        } else if (value instanceof dsviper.ValueVariant) {
            if (!value.type().equals(mt.type<v.typeSuffix>())) {
                throw new TypeError("value is not a <v.dsmType>");
            }
            super(value);
        } else if (value instanceof Proxy) {
            super(new dsviper.ValueVariant(mt.type<v.typeSuffix>(), value.vprValue));
        } else {
            super(new dsviper.ValueVariant(mt.type<v.typeSuffix>(), value as dsviper.InputValue));
        }
    }

    static wrap(value: dsviper.OutputValue): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(dsviper.ValueVariant.cast(value as dsviper.Value));
    }

    <v.members:variant_member();separator="\n">

    copy(): <v.bindingType.proxy> {
        return new <v.bindingType.proxy>(this.vprValue.copy());
    }

    static decode(blob: dsviper.ValueBlob, streamCodecInstancing?: dsviper.StreamCodecInstancing): <v.bindingType.proxy> {
        const value = dsviper.ValueVariant.cast(dsviper.Value.decode(blob, mt.type<v.typeSuffix>(), md.definitions(), streamCodecInstancing ?? dsviper.Codec.STREAM_BINARY));
        return new <v.bindingType.proxy>(value);
    }
}

>>

variant_member(vm) ::= <<
is<vm.bindingType.proxy;format="uf">(): boolean {
    return (this.vprValue.unwrap(false) as dsviper.Value).type().equals(mt.type<vm.typeSuffix>());
}

get<vm.bindingType.proxy;format="uf">(): <vm.bindingType:tsType()> {
    if (!this.is<vm.bindingType.proxy;format="uf">()) {
        throw new Error("variant does not hold a <vm.bindingType:tsType()>");
    }
    return <vm.bindingType:wrap({this.vprValue.unwrap()})>;
}

set<vm.bindingType.proxy;format="uf">(value: <vm.bindingType:tsType()>): void {
    this.vprValue.wrap(<vm.bindingType:unwrap("value")>, mt.type<vm.typeSuffix>());
}
>>
