main(m) ::= <<
// Generated by kibo from <m.namespace>. Do not edit by hand.
// Templates: kibo-template-viper 1.2.1 (MIT).
// Runtime: this file imports `@digitalsubstrate/dsviper`, distributed under
// LicenseRef-DigitalSubstrate-Commercial-1.2. Commercial use
// requires a Commercial Licence from Digital Substrate.
// <m.generated>

/**
 * Getters and setters for attachments defined for <m.namespace>.
 * One exported object per attachment, with short methods (get/set/has/...).
 */

import dsviper from "@digitalsubstrate/dsviper";
import * as mp from "./path.js";
import * as mt from "./value_type.js";
import * as d from "./data.js";

<m.nameSpaces:namespace();separator="\n">
>>

namespace(ns) ::= <<
// Namespace <ns.name>
<ns.attachments:attachment();separator="\n">
>>

tsLeaf ::= [
    "bool": "boolean",
    "uint8": "number", "uint16": "number", "uint32": "number",
    "int8": "number", "int16": "number", "int32": "number",
    "uint64": "bigint", "int64": "bigint",
    "float": "number", "double": "number",
    "string": "string",
    "blob": "dsviper.ValueBlob",
    "blob_id": "dsviper.ValueBlobId",
    "commit_id": "dsviper.ValueCommitId",
    "uuid": "dsviper.ValueUUId",
    "Any": "dsviper.ValueAny",
    "void": "void"
]

dtype(t) ::= <%<if(t.useProxy)>d.<t.proxy><else><tsLeaf.(t.proxy)><endif>%>
unwrap(t, e) ::= <%<if(t.useProxy)><e>.vprValue<else><e><endif>%>

attachmentType(a) ::= <%mt.attachment_<a.namespace>_<a.identifier>()%>
keySet(a) ::= <%d.Set_<a.keyType.pythonType.proxy>%>
keyProxy(a) ::= <%d.<a.keyType.pythonType.proxy>%>
docOptional(a) ::= <%d.Optional_<a.documentType.pythonType.proxy>%>
path(a, sf) ::= <%mp.<a.namespace>_Path_<a.documentType.structure.name>.<sf.name>%>

attachment(a) ::= <<
// <a.representation>
export const <a.identifier;format="lf"> = {
    keys(attachmentGetting: dsviper.AttachmentGetting): <a:keySet()> {
        const attachment = <a:attachmentType()>;
        return new <a:keySet()>(attachmentGetting.keys(attachment));
    },

    diffKeys(current: dsviper.AttachmentGetting, other: dsviper.AttachmentGetting): [<a:keySet()>, <a:keySet()>, <a:keySet()>, <a:keySet()>] {
        const attachment = <a:attachmentType()>;
        const [added, removed, different, same] = dsviper.AttachmentGetting.diffKeys(current, other, attachment);
        return [new <a:keySet()>(added), new <a:keySet()>(removed), new <a:keySet()>(different), new <a:keySet()>(same)];
    },

    has(attachmentGetting: dsviper.AttachmentGetting, key: <a:keyProxy()>): boolean {
        const attachment = <a:attachmentType()>;
        return attachmentGetting.has(attachment, key.vprValue);
    },

    get(attachmentGetting: dsviper.AttachmentGetting, key: <a:keyProxy()>): <a:docOptional()> {
        const attachment = <a:attachmentType()>;
        return new <a:docOptional()>(attachmentGetting.get(attachment, key.vprValue));
    },

    set(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: <a.documentType.pythonType:dtype()>): void {
        const attachment = <a:attachmentType()>;
        attachmentMutating.set(attachment, key.vprValue, <a.documentType.pythonType:unwrap("value")>);
    },

    diff(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: <a.documentType.pythonType:dtype()>, recursive: boolean = false): void {
        const attachment = <a:attachmentType()>;
        attachmentMutating.diff(attachment, key.vprValue, <a.documentType.pythonType:unwrap("value")>, recursive);
    },

    <if(a.documentType.isStructure)><a.documentType.structure.fields:field(a);separator="\n"><elseif(a.documentType.field.isNotBox)><a:document()><endif>

    *enumerate(attachmentGetting: dsviper.AttachmentGetting): Generator\<[<a:keyProxy()>, <a.documentType.pythonType:dtype()>]> {
        for (const key of this.keys(attachmentGetting)) {
            const document = this.get(attachmentGetting, key).unwrap();
            yield [key, document];
        }
    },
};

>>

field(sf, a) ::= <<
set<sf.name;format="uf">(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: <sf.pythonType:dtype()>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.update(attachment, key.vprValue, <a:path(sf)>, <sf.pythonType:unwrap("value")>);
},
<if(sf.field.isSet)><sf:field_set(a)><elseif(sf.field.isMap)><sf:field_map(a)><elseif(sf.field.isXArray)><sf:field_xarray(a)><endif>
>>

field_set(sf, a) ::= <<
union<sf.name;format="uf">(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: d.<sf.pythonType.proxy>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.unionInSet(attachment, key.vprValue, <a:path(sf)>, value.vprValue);
},
subtract<sf.name;format="uf">(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: d.<sf.pythonType.proxy>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.subtractInSet(attachment, key.vprValue, <a:path(sf)>, value.vprValue);
},
>>

field_map(sf, a) ::= <<
union<sf.name;format="uf">(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: d.<sf.pythonType.proxy>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.unionInMap(attachment, key.vprValue, <a:path(sf)>, value.vprValue);
},
subtract<sf.name;format="uf">(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: d.Set_<sf.field.pythonKeyType.proxy>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.subtractInMap(attachment, key.vprValue, <a:path(sf)>, value.vprValue);
},
update<sf.name;format="uf">(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: d.<sf.pythonType.proxy>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.updateInMap(attachment, key.vprValue, <a:path(sf)>, value.vprValue);
},
>>

field_xarray(sf, a) ::= <<
insert<sf.name;format="uf">(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, beforePosition: dsviper.ValueUUId, newPosition: dsviper.ValueUUId, value: <sf.field.pythonElementType:dtype()>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.insertInXarray(attachment, key.vprValue, <a:path(sf)>, beforePosition, newPosition, <sf.field.pythonElementType:unwrap("value")>);
},
update<sf.name;format="uf">(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, position: dsviper.ValueUUId, value: <sf.field.pythonElementType:dtype()>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.updateInXarray(attachment, key.vprValue, <a:path(sf)>, position, <sf.field.pythonElementType:unwrap("value")>);
},
remove<sf.name;format="uf">(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, position: dsviper.ValueUUId): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.removeInXarray(attachment, key.vprValue, <a:path(sf)>, position);
},
>>

document(a) ::= <<
<if(a.documentType.field.isSet)><a:document_set()><elseif(a.documentType.field.isMap)><a:document_map()><elseif(a.documentType.field.isXArray)><a:document_xarray()><endif>
>>

document_set(a) ::= <<
union(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: <a:docOptional()>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.unionInSet(attachment, key.vprValue, mp.Path_Root, value.vprValue);
},
subtract(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: d.<a.documentType.pythonType.proxy>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.subtractInSet(attachment, key.vprValue, mp.Path_Root, value.vprValue);
},
>>

document_map(a) ::= <<
union(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: d.<a.documentType.pythonType.proxy>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.unionInMap(attachment, key.vprValue, mp.Path_Root, value.vprValue);
},
subtract(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: d.Set_<a.documentType.field.pythonKeyType.proxy>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.subtractInMap(attachment, key.vprValue, mp.Path_Root, value.vprValue);
},
update(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, value: d.<a.documentType.pythonType.proxy>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.updateInMap(attachment, key.vprValue, mp.Path_Root, value.vprValue);
},
>>

document_xarray(a) ::= <<
insert(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, beforePosition: dsviper.ValueUUId, newPosition: dsviper.ValueUUId, value: <a.documentType.field.pythonElementType:dtype()>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.insertInXarray(attachment, key.vprValue, mp.Path_Root, beforePosition, newPosition, <a.documentType.field.pythonElementType:unwrap("value")>);
},
update(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, position: dsviper.ValueUUId, value: <a.documentType.field.pythonElementType:dtype()>): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.updateInXarray(attachment, key.vprValue, mp.Path_Root, position, <a.documentType.field.pythonElementType:unwrap("value")>);
},
remove(attachmentMutating: dsviper.AttachmentMutating, key: <a:keyProxy()>, position: dsviper.ValueUUId): void {
    const attachment = <a:attachmentType()>;
    attachmentMutating.removeInXarray(attachment, key.vprValue, mp.Path_Root, position);
},
>>
